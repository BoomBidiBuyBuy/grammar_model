(+ ) 1. Allow to specify type of node in rules.
      For example: only CIFS_J is required for NFS_WS
(-)  2. In some cases passed arguments are set for the wrong node
     3. Add upload/download methods
     4. model validation
     5. error handling and problem-path